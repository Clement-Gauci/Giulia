# Fermetures exceptionnelles (congés, jours fériés…).
# Chaque entrée ferme l'établissement de `from` à `until` INCLUS ; le badge
# affiche « En congés » et la date de réouverture (= lendemain de `until`).
# `reason` est purement documentaire.
# (Aucune fermeture programmée pour le moment.)
[]
